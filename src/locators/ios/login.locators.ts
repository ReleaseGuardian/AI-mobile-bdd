/**
 * Locators for the BStackSampleApp (iOS) login screen.
 * Based on BrowserStack's publicly documented sample-app selectors (default demo
 * credentials: demouser / testingisfun99). Re-verify with Appium Inspector against your
 * actual app build before relying on these in CI — third-party app IDs can drift across
 * app versions.
 */
export const IOSLoginLocators = {
  loginScreenAnchor: '~Sign In',
  usernameInput: '~Username',
  passwordInput: '~Password',
  loginButton: '~Sign In',
  homeScreenAnchor: '~Product Store',
};
